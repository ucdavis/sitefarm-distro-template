name: SiteFarm Sub-profile
type: profile
description: 'A fast and feature-rich UC Davis Drupal distribution. A sub-profile of SiteFarm Seed'
# version: VERSION
core: 8.x

# Force the installation of this profile since it is a distribution
distribution:
  name: SiteFarm

# Base profile
base profile: sitefarm_seed

# Required modules
# dependencies:
  # core
  # contrib
  # SiteFarm

themes:
  - seven
  - bartik
